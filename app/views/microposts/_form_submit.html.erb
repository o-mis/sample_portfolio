<%
=begin%>
 TODO 不要なので削除 
<%
=end%>
<%= form_with(model: micropost, local: true) do |form| %>
  <div class="actions">
    <%= form.submit class: "btn rounded-pill btn-micropost" %>
  </div>
<% end %>